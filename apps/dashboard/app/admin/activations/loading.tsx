export default function ActivationLoading() {
  return <div aria-busy="true" aria-label="Memuat aktivasi desa" className="mx-auto max-w-6xl motion-safe:animate-pulse space-y-6"><span className="sr-only">Memuat aktivasi desa…</span><div className="h-24 rounded-lg bg-muted" /><div className="h-80 rounded-lg bg-muted" /></div>;
}
